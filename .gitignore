# Build artifacts
*.tar.gz
build_*.log
output_build.log
output_install.log

# Status / completion scratch files
STATUS_REPORT.md
COMPLETION_REPORT.md

# OS / editor
.DS_Store
*.swp
*.swo

# Secrets (added 2026-04-21 after public-key leak)
.env
.env.local
.env.*.local

# MCP config (contains API key)
.mcp.json

# Any *.local scratch (pre-scrub backups, editor sidecars, etc.)
*.local
